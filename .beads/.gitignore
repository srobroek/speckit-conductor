# Beads gate lock (runtime artifact)
*.gate.lock*

# Added by bd (missing required patterns)
*.db?*
.env
redirect
last-touched
bd.sock.startlock
.sync.lock
export-state/
export-state.json
last_pull
dolt/
embeddeddolt/
proxieddb/
ephemeral.sqlite3
dolt-server.pid
dolt-server.log
dolt-server.lock
dolt-server.port
dolt-server.activity
daemon.*
*.lock
*.corrupt.backup/
.beads-credential-key
proxied_server_client_info.json
.local_version
backup/
